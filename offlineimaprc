[general]
pythonfile = ~/.offlineimap.py
accounts = charite
maxsyncaccount = 6
ui = ttyui

[Account charite]
localrepository = charite-local
remoterepository = charite-remote
autorefresh = 1

[Repository charite-local]
type = Maildir
localfolders = ~/.mail_imap_offline/charite
set = "/"

[Repository charite-remote]
type = IMAP
# remotehost = exchange-imap.charite.de
remotehost = email.charite.de
# remotehost = mproxy.charite.de
remoteuser = holtgrem
remotepasseval = get_password()
#ssl = yes
starttls = yes

# cert_fingerprint = 5b9287f998f6be18543e3c017a489297d551ea49
cert_fingerprint = 96857142e030b3cfe418063af9ccc6c99678c737
# cert_fingerprint = ef181474cd56c9ae4f55ba1603b4d0e7fb6b7d26
idlefolders = ['INBOX', 'Backup', 'BIH Cluster', 'BIH Helpdesk/cubi-demux', 'BIH Helpdesk/hpc-helpdesk', 'mailing-lists/bih-cluster']
folderfilter = lambda folder: not re.search('(^Calendar$|^Contacts$|^Tasks$|^Notes$|^search$|^local-archive|^Trash)', folder)

